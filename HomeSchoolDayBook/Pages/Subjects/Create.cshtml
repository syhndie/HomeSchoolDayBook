@page
@model HomeSchoolDayBook.Pages.Subjects.CreateModel

@{
    ViewData["Title"] = "Create New Subject";
}

<h2>@ViewData["Title"]</h2>

<partial name="SubjectPartial" model="Model.SubjectVM" />

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
