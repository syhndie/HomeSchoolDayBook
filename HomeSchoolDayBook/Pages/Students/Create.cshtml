@page
@model HomeSchoolDayBook.Pages.Students.CreateModel

@{
    ViewData["Title"] = "Create a Student";
}

<h4 class="text-danger">@Model.DidNotSaveMessage </h4>

<h2>Create</h2>
            
<partial name="StudentPartial" model="Model.Student">

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
