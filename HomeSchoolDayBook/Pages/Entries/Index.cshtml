@page
@model HomeSchoolDayBook.Pages.Entries.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table" id="entryindex">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Entries[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entries[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entries[0].ComputedTimeSpent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Entries) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ComputedTimeSpent)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>