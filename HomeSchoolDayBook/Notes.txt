FIX COOKIE POLICY - are cookies being used when the user has not yet accepted cookies?

need more consistent error logging and handling

add Admin pages
	confirm email addresses manually
	resend confirmation email
	delete users and all users data
	see all data
	here is some related code:
            var adminList = await _userManager.GetUsersInRoleAsync(Constants.AdminRoleName);
            if (adminList.Select(u => u.Id).Contains(userId))
            {
                Students = await _context.Students
                    .ToListAsync();
            }

add grades??? point system???
	on entry create/edit page - link to another page to add points

	point-adding page shows each student-subject pairing
	for each pairing, you can add points earned and points available

	students index page has details link

	student details page shows total points earned, total points available, and percentage of points earned for each subject
	you can click on each subject row show break-out rows that list each entry where points were available
	each break-out row shows entry date, entry title, points earned, points available, percent of points earned, and a link to edit the entry

	also include a points report - choose date range, subjects, and students
	for each student, list all subjects chosen
	for each subject, list points earned, points available, and percentage of points
	